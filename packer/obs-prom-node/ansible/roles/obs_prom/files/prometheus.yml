# PLACEHOLDER -- overwritten by terraform/envs/obs-prom/role-overlay-prom-config.tf
# at apply time. The real config carries the cluster-wide scrape target list
# rendered from nexus-platform-plan/docs/infra/vms.yaml + the Alertmanager
# alerting block + TLS via the Vault Agent-rendered cert.
#
# Phase 0.I.1, ADR-0038.

global:
  scrape_interval: 30s
  evaluation_interval: 30s
  external_labels:
    cluster: nexus-observability
    replica: placeholder

scrape_configs:
  - job_name: prometheus_self
    static_configs:
      - targets: ['localhost:9090']

  - job_name: alertmanager_self
    static_configs:
      - targets: ['localhost:9093']

# Real config rendered post-apply.
